# For local dev the app proxies /api to http://localhost:8000 (see next.config.js).
# For production (Vercel), set this to your deployed Render backend URL:
# NEXT_PUBLIC_API_BASE=https://decisionflow-backend.onrender.com/api
NEXT_PUBLIC_API_BASE=
